<UserControl x:Class="NVVM_InternetMarket.View.ItemsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NVVM_InternetMarket.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <Grid>
        <StackPanel Name="ProductInfoView" Orientation="Horizontal">
            <TextBlock Text="{Binding }" />
            <TextBlock Text=" [" Foreground="Blue" />
            <TextBlock Text="{Binding SubElements.Count}" Foreground="Blue" />
            <TextBlock Text="]" Foreground="Blue" />
        </StackPanel>
    </Grid>
</UserControl>
